Remove Duplicates from an array :
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        if len(nums)==0:
            return 0
        elif len(nums)==1:
            return 1
        else:
            i=0
            for j in range(1,len(nums)):
                if nums[j]!=nums[i]:
                    nums[i+1]=nums[j]
                    i+=1
            return i+1
